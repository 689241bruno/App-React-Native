import React from "react";
import {
  Text,
  View,
  StyleSheet,
  ScrollView,
  Pressable,
  Image,
  TouchableOpacity,
} from "react-native";

export default function Perfil() {
  return (
    <ScrollView>
      <View></View>
    </ScrollView>
  );
}
